# Changesets

Changesets record future version intent only. This repository deliberately has no publish workflow. Run `pnpm changeset:status` to inspect pending intent; do not run `changeset publish`.
