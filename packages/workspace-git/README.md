# `@relvo-labs/agent-workspace-git`

Git-backed workspace boundary with an injected command runner. Borrowed workspaces permit read-only inspection only; this package never discovers or owns credentials.
