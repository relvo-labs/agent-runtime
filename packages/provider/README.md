# `@relvo-labs/agent-provider`

Neutral provider SPI, structured capability descriptors, recovery-record boundary, and deterministic scripted test provider. Provider handles remain in process and never enter public wire DTOs.
