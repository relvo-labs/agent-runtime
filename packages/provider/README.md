# `@relvo-labs/agent-provider`

Neutral provider SPI, immutable validated capability descriptors, timestamp-free validated run completion, retry-safe provider disposal, recovery-record boundary, and deterministic scripted test provider. Provider handles remain in process and never enter public wire DTOs.
