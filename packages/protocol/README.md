# `@relvo-labs/agent-protocol`

Authoritative Zod schemas, inferred TypeScript types, and generated JSON Schema for the Relvo Agent Runtime wire contract. The `/schemas` export exposes deterministic generated schemas for non-TypeScript consumers.
