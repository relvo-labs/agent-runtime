# `@relvo-labs/agent-provider-codex`

Reserved package boundary for a future structured Codex adapter. Foundation v0.4 exports only identifiers and a factory type; it does not execute Codex, authenticate, spawn a CLI, or make model calls.

See [`docs/provider-development.md`](../../docs/provider-development.md) before implementing this boundary.
