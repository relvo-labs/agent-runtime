# `@relvo-labs/agent-provider-claude`

Reserved package boundary for a future structured Claude adapter. Foundation v0.4 exports only identifiers and a factory type; it does not execute Claude, authenticate, spawn a CLI, or make model calls.

See [`docs/provider-development.md`](../../docs/provider-development.md) before implementing this boundary.
