# `@relvo-labs/agent-executor`

Consumer-facing `AgentExecutor` contract plus a framework-neutral conformance kit. It defines commands, projection reads, replay-then-live subscriptions, and shutdown without choosing a provider or transport.
